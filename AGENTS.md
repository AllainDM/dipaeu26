# Инструкции для агента (opencode)

## О проекте

Статический сайт-сателлит для текстовой дипломатической игры (medieval Europe ~1000 AD).
Хостится на GitHub Pages: `https://allaindm.github.io/dipaeu26`
Стек: **HTML + CSS + vanilla JavaScript** (без npm, сборщиков, фреймворков).
Язык сайта — **русский**.

**Команда:**
- Ведущий (ГМ): Виталий Харченко
- Разработчик: Сергей Куропятников (vk.com/allain)
- Поддержка: Саша Доронин

---

## 1. Обновление лога изменений

При любых изменениях сайта необходимо добавить запись в `log.html`.

- Запись вставляется после `<p>История изменений:</p>`, перед первой существующей записью.
- Формат: `<p>DD.MM.YYYY: Описание изменения.</p>`
- Дата — текущая, формат `день.месяц.год` (например `24.06.2026`).
- Описание — краткое, **на русском языке**, в стиле существующих записей.
- **Все изменения одного дня группируются в одну строку** — описания разделяются точкой с запятой и пробелом.
- Если в тот же день запись уже существует, дополни её (не создавай дубликат даты).

**Примеры:**
```html
<p>24.06.2026: Закомментированы вкладки правил и игроков. Добавлены 14-24 ходы.</p>
<p>15.02.2026: Добавлены карты: политическая, климат, этносы, ресурсы. Добавлен список игроков с ссылками.</p>
```

---

## 2. Структура проекта

```
├── index.html              # Главная (правила — закомментированы)
├── turns.html              # Ходы (SPA-страница)
├── turns.js                # Данные ходов (события, карты)
├── maps.html               # Карты (с переключением)
├── map-script.js           # Логика переключения карт
├── tables.html             # Таблицы (с переключением)
├── tables-script.js        # Логика переключения таблиц
├── players.html            # Статистика игроков
├── players.js              # Данные игроков (42 страны)
├── innovation.html         # Все инновации
├── innovation.js           # Данные инноваций (mil, ekonom, social)
├── distance.html           # Считалка расстояний
├── distance.js             # Координаты городов + логика
├── links.html              # Ссылки
├── log.html                # Лог изменений
├── feedback.html           # Отзыв (закомментирован из меню)
├── style.css               # Единый файл стилей
├── analytics.js            # Сбор аналитики — НЕ ТРОГАТЬ
├── maps/                   # Карты (polit/*.png, climate.png и др.)
├── tables/                 # Таблицы (*.png, *.jpg)
└── turns/                  # HTML-файлы ходов (turn00/ … turn15/)
```

---

## 3. Соглашения по коду

- **Язык**: контент, комментарии, коммиты — **русский**.
- **HTML**: `<!DOCTYPE html>`, `<meta charset="UTF-8">`, `<link rel="stylesheet" href="style.css">`, `<script src="analytics.js"></script>` в конце `<body>`.
- **Навигация**: `<header>` → `<ul class="mainmenu">` — копируется на все страницы.
- **Активные пункты меню**: `Ходы`, `Карты`, `Таблицы`, `Ссылки`, `Лог`.
- **Закомментированы** (не трогать без явной просьбы): `Правила`, `Игроки`, `Отзыв`.
- **CSS**: единый `style.css`. Локальные стили — через `<style>` в `<head>`.
- **JS**: vanilla, `const`/`let`, отступы 4 пробела.
- **Закомментированный код**: не удалять, не раскомментировать без явной просьбы.

---

## 4. Git

- Сообщения коммитов — на русском, кратко, информативно.
- `log.html` обновляется в том же коммите, что и изменения.
- Примеры: `"Добавлен 10-й ход"`, `"Обновлена политическая карта"`, `"Фикс стилей для кнопок в таблицах и картах"`.
